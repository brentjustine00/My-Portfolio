# Brent Barbadillo Portfolio

Static portfolio built with React (Vite + TypeScript) and TailwindCSS, based on the provided Figma design.

## Getting Started

```bash
npm install
npm run dev
```

## Notes

- Update social links and email in `src/components/Contact.tsx`.
- Replace project entries in `src/data/projects.ts` with your real projects.
```
